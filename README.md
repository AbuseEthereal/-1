# -1
Программа запрашивает диапазон X и шаг. Выводит таблицу с номером строки, X и значением min(log_(|num/10|+4)((1-num)/cos(x-num)), sin(x))/num, num=23. При ошибках (деление на 0, логарифм, cos=0) — сообщение об ошибке. Две пользовательские функции возвращают значения для min.
